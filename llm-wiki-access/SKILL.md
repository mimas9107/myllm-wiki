---
name: llm-wiki-access
description: 存取並檢索個人的 LLM Wiki 知識庫，以回答使用者的問題或進行知識關聯。
---

# llm-wiki-access 執行協定

## 目標
讓 Agent 能夠安全、有效率地導覽並讀取使用者的本機知識庫。

## 執行流程
1. **索引導航**: 所有的檢索必須從讀取 `wiki/INDEX.md` 開始，從中尋找與 User 問題相關的頁面連結。
2. **追蹤連結**: 使用工具讀取對應的 `wiki/*.md` 檔案。
3. **溯源**: 如果 Wiki 頁面內有提供指向 `raw/` 原始檔案的連結，且需要更詳細的技術規格時，再進一步去讀取 `raw/` 中的內容。

## 邊界條件 (Constraints)
1. **唯讀存取**: 此技能**僅限於讀取**，嚴禁修改 `wiki/` 或 `raw/` 中的任何內容。
2. **尊重原始檔案**: `raw/` 目錄是絕對唯讀的，不可變更。
